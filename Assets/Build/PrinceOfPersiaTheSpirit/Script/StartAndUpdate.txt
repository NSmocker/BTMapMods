local StartAndUpdate = {
   
}


--[[
function StartAndUpdate:Awake()
    self.scheId = nul
    self.speed =1 
    self.schedulerInterval = 0.5
    self.timeBeforeStopping = -1
    self.timeBeforeStarting = 0
end


]]--

function StartAndUpdate:Start()
	print("START AND UPDATE WORKING")
    local current_time = CS.UnityEngine.Time.time
    current_time = tostring(current_time)
    
    CS.UnityEngine.Debug.Log(current_time .."Time After Launch" )
    --print("[StartAndupdate]"+ CS.UnityEngine.Time.deltaTime.ToString() + "Start activated")   
    local text_sample = CS.UnityEngine.GameObject.Find('Text (TMP)')
    local text_component = text_sample:GetComponent('TextMeshPro')
    text_component.text = "your new text"
    
    
end






function StartAndUpdate:OnTriggerEnter()
      print("Player touched me")
end


return Class(nil, nil, StartAndUpdate)